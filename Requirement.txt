Agent Need to Cabale of Either Need to use Image or normal text.


I mean For now build like this

If user asks question try to answer if it not able to answer without the image then pic the image at that point have along with that you need to pass the conversation to it and then it can return you the response am thinking to have

And Message Need to be just like how we chat normally with gpt there some times will give only query and some times query + image right but here it can decide when to have image and see forexample in a conversation at turn 2 it processes a image + query then in turn 4 if i ask any other query related to it id does need to be capable of understanding conversation with images above to just like how we get responses in gpts and if not able to answer then need to pick image these kind of dynamic desiion picking require

Ignore and remove all previous unnessary code this is the core functionality am expecting now